# NOTICE

이 프로젝트는 브라우저의 Web APIs(FileReader, localStorage, Web Speech API 등)를 사용합니다.

- TTS(발음)는 브라우저/OS에 설치된 음성(voices)에 따라 지원 범위가 달라질 수 있습니다.
- localStorage 저장 데이터는 **해당 브라우저/기기**에 저장됩니다(다른 기기로 자동 동기화되지 않음).
